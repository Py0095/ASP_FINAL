@page
@model ProjetFinal.Pages_DGI.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DGI[0].contravention)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DGI[0].Montant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DGI[0].Remarque)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DGI[0].Date_paiement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DGI) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.contravention.No_Fiche)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Montant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Remarque)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date_paiement)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
