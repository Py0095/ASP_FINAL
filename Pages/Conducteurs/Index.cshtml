@page
@model ProjetFinal.Pages_Conducteurs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Conducteur[0].Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conducteur[0].Prenom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conducteur[0].Sexe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conducteur[0].Nif)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Conducteur) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sexe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nif)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.No_Dossier">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.No_Dossier">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.No_Dossier">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
